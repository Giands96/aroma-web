// Utility functions — pure, no side effects, no Supabase calls.
//
// whatsapp.ts  — buildWhatsAppUrl(phone, items, isPersonalized?)
// currency.ts  — formatCurrency(amount)
// slug.ts      — generateSlug(name)
